export default function Home() {
  return (
    <main>
      <h1>Velo</h1>
      <p>Cash in / cash out — P0 build starts here.</p>
      {/* TODO (Core Retail Flow P0): one identity per device, real
          nearby-provider list from the backend, real wallet balance. */}
    </main>
  );
}
